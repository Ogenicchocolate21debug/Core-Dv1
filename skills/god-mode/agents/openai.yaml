interface:
  display_name: "GOD MODE"
  short_description: "Hybrid outbound toolkit for cloud and local work"
  icon_small: "./assets/god-mode-48.png"
  icon_large: "./assets/god-mode-256.png"
  default_prompt: "Use $god-mode to operate through the best available cloud or local tools, return a concise verified result, and never hard-delete data or rewrite history."
policy:
  allow_implicit_invocation: true
